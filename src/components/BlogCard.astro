---
import blogData from '../data/blog.json';
---

<section id="blog" class="bg-gray-950 text-white py-20 px-6">
  <div class="max-w-7xl mx-auto text-center mb-12">
    <h2 class="text-4xl font-bold mb-4">Últimas noticias</h2>
    <p class="text-gray-300">Mantente al día con lo último sobre Tesla en Chile y el mundo.</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-7xl mx-auto">
    {blogData.map((post) => (
      <article class="bg-gray-900 rounded-2xl overflow-hidden hover:scale-105 transition">
        <a href={`/blog/${post.id}`}>
          <img src={post.imagen} alt={post.titulo} class="w-full h-48 object-cover" />
        </a>
        <div class="p-6">
          <h3 class="text-xl font-semibold mb-2">{post.titulo}</h3>
          <p class="text-gray-400 mb-4">{post.resumen}</p>
          <a href={`/blog/${post.id}`} class="text-yellow-400 hover:underline">Leer más →</a>
        </div>
      </article>
    ))}
  </div>
</section>
